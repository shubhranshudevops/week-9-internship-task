{
  "name": "jenkins-docker-demo",
  "version": "1.0.0",
  "description": "Jenkins CI/CD demo application",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node -e \"console.log('Automated test passed')\"",
    "build": "mkdir -p dist && cp server.js dist/server.js && cp package.json dist/package.json"
  },
  "dependencies": {
    "express": "^5.1.0"
  },
  "overrides": {
    "cross-spawn": "^7.0.5",
    "glob": "^11.1.0",
    "ip-address": "^10.3.1",
    "minimatch": "^10.2.3",
    "brace-expansion": "^5.0.9"
  }
}
